# Consolidated dependencies for the whole repository.
# Per-notebook subsets live in each paper's folder (e.g. Transformer/requirements.txt)
# if you only want to set up the environment for a single notebook.
numpy==2.4.5
matplotlib==3.10.9
torch==2.12.0
torchvision==0.27.0
transformers==5.12.1
datasets==5.0.0
spacy==3.8.14
click==8.4.1
notebook==7.5.6
tqdm==4.68.3
